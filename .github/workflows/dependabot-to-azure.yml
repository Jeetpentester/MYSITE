name: Sync Dependabot Alerts to Azure Boards

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'  # every 6 hours

jobs:
  sync_dependabot_alerts:
    runs-on: ubuntu-latest

    steps:
      - name: 🧪 Fetch Dependabot Alerts
        env:
          GH_TOKEN: ${{ secrets.GH_DEPENDABOT_PAT }}
          GH_REPO: ${{ github.repository }}
        run: |
          echo "🔐 Fetching Dependabot alerts from $GH_REPO"

          curl -s -H "Authorization: token $GH_TOKEN" \
               -H "Accept: application/vnd.github+json" \
               "https://api.github.com/repos/$GH_REPO/dependabot/alerts?state=open" \
               -o alerts.json

          echo "📄 Raw alerts:"
          cat alerts.json

          echo "🧪 Check if alerts.json is a valid array"
          IS_ARRAY=$(jq 'if type=="array" then true else false end' alerts.json)
          echo "IS_ARRAY=$IS_ARRAY" >> $GITHUB_ENV

      - name: 🔁 Create Azure Boards Tasks from Alerts
        if: env.IS_ARRAY == 'true'
        env:
          AZURE_ORG_URL: https://dev.azure.com/devsecopsjeet
          AZURE_PROJECT: MYSITE
          AZURE_DEVOPS_PAT: ${{ secrets.AZURE_DEVOPS_PAT }}
        run: |
          echo "📤 Processing alerts..."

          jq -c '.[]' alerts.json | while read -r alert; do
            SEVERITY=$(echo "$alert" | jq -r '.security_advisory.severity')
            PACKAGE=$(echo "$alert" | jq -r '.dependency.package.name')
            IDENTIFIER=$(echo "$alert" | jq -r '.security_advisory.ghsa_id')
            DESC=$(echo "$alert" | jq -r '.security_advisory.summary')
            URL=$(echo "$alert" | jq -r '.html_url')

            TITLE="⚠️ $SEVERITY vulnerability in $PACKAGE ($IDENTIFIER)"

            echo "➡️ Creating task: $TITLE"

            AUTH_HEADER=$(echo -n ":$AZURE_DEVOPS_PAT" | base64 | tr -d '\n')

            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "$AZURE_ORG_URL/$AZURE_PROJECT/_apis/wit/workitems/\$Task?api-version=7.0" \
              -H "Content-Type: application/json-patch+json" \
              -H "Authorization: Basic $AUTH_HEADER" \
              -d "[{
                    \"op\": \"add\",
                    \"path\": \"/fields/System.Title\",
                    \"value\": \"$TITLE\"
                  },{
                    \"op\": \"add\",
                    \"path\": \"/fields/System.Description\",
                    \"value\": \"GitHub Dependabot alert: <a href='$URL'>$URL</a><br/><br/>$DESC\"
                  }]")

            STATUS=$(echo "$RESPONSE" | tail -n1)
            if [[ "$STATUS" == "200" || "$STATUS" == "201" ]]; then
              echo "✅ Task created for $PACKAGE"
            else
              echo "❌ Failed to create task. Status: $STATUS"
              echo "$RESPONSE" | sed '$d'
            fi
          done

      - name: ⚠️ Log if API returned non-array data
        if: env.IS_ARRAY != 'true'
        run: |
          echo "⚠️ GitHub API did not return a valid array. Check alerts.json:"
          cat alerts.json